services:
  - type: web
    name: catalyst-blockchain-core
    env: python
    buildCommand: pip install -r requirements.render.txt
    startCommand: uvicorn app.api.main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
