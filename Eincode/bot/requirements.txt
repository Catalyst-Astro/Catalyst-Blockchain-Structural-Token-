# ═══════════════════════════════════════════════════════════════
# Catalyst Threema Bot — Dependencias
# BELL 13450.50 | Pentetraktys 4D
# ═══════════════════════════════════════════════════════════════

# Core
openai>=1.0.0          # DeepSeek API (compatible con OpenAI SDK)
fastapi>=0.110.0       # REST API server
uvicorn[standard]>=0.29.0  # ASGI server

# Threema Gateway (opcional — solo si usas Threema)
threema.gateway>=3.0.0

# Threema Web Bridge (opcional — para conexión directa sin Gateway)
playwright>=1.40.0       # Automatización de navegador para Threema Web

# Utilidades
python-dotenv>=1.0.0   # Variables de entorno desde .env
pydantic>=2.0.0        # Validación de datos
libnacl>=2.0.0         # NaCl crypto (requerido por threema.gateway)
