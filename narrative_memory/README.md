# Narrative Ledger Seeds

- `narrative_ledger.jsonl` is a local runtime ledger and is ignored by Git.
- `narrative_ledger.seed.jsonl` is the tracked placeholder for a clean starting point.
- Set `CATALYST_NARRATIVE_LEDGER_PATH` to relocate the live ledger outside the repo.
